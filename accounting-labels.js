import {STORAGE_KEY} from './records.js?v=20260907-1430';
import {ledgerMetrics} from './calculations.js?v=20260907-1430';
const fmt=n=>Number.isFinite(+n)?(+n).toLocaleString('en-US',{maximumFractionDigits:2}):'—';
const load=()=>{try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}')}catch{return {}}};
function card(label,value,note=''){return `<div class="card kpi"><div class="label">${label}</div><div class="value">${fmt(value)} U</div>${note?`<div class="subtle">${note}</div>`:''}</div>`}
function patch(){const state=load(),m=ledgerMetrics(state);document.querySelectorAll('.capital-strip').forEach(el=>{el.innerHTML=card('第一倉投入本金',m.firstCapital,`期初 31,664 · 後續入金 ${fmt(m.byWarehouse.第一倉.deposits)} · 出金 ${fmt(m.byWarehouse.第一倉.withdrawals)}`)+card('第二倉期初資產基準',m.secondOpeningAssetBaseline,'2026/09/07 依當日資產市值建立；不是歷史成本')+card('期初後淨入金',m.postBaselineNetFlow,`第二倉入金 ${fmt(m.byWarehouse.第二倉.deposits)} · 出金 ${fmt(m.byWarehouse.第二倉.withdrawals)}`)+card('累積非交易收益',m.income,'Earn / Staking / 空投 / Cashback；不列入本金');});const ledger=document.querySelector('#ledger');if(ledger){const cards=[...ledger.querySelectorAll(':scope > .grid:first-child .card')];if(cards[0])cards[0].innerHTML=`<div class="label">第一倉投入本金</div><div class="value">${fmt(m.firstCapital)} U</div><div class="subtle">後續入 ${fmt(m.byWarehouse.第一倉.deposits)} · 出 ${fmt(m.byWarehouse.第一倉.withdrawals)}</div>`;if(cards[1])cards[1].innerHTML=`<div class="label">第二倉期初資產基準</div><div class="value">${fmt(m.secondOpeningAssetBaseline)} U</div><div class="subtle">後續淨入金 ${fmt(m.postBaselineNetFlow)} U</div>`;if(cards[2])cards[2].innerHTML=`<div class="label">非交易收益</div><div class="value good">${fmt(m.income)} U</div><div class="subtle">由「收益 / 空投」紀錄累加</div>`;}}
}
let queued=false;const schedule=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;patch()})};new MutationObserver(schedule).observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('load',schedule);schedule();
